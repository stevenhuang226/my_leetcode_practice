#include "shdstd-cpp.h"

using namespace std;

/*
 * auth: stevenhuang.dog@gmail.com
 * help: chatgpt
 */

class Solution {
	int exp_size = 1e5 + 8;

	public:
		int longestBalanced(vector<int> &nums) {
			int size = nums.size();

			vector<int> prev_index(size, 0);

			for (int r = 0; r < size; ++r) {
				int num = nums[r];
			}
		}
};
