#include <stdio.h>
#include <math.h>
int maxArea(int* height, int heightSize);
int main()
{
	int height[9] = {1,8,6,2,5,4,8,3,7};
	int max = maxArea(height, 9);
	printf("%d\n", max);
	return 0;
}
int maxArea(int* height, int heightSize)
{
	int maxNum;
	return maxNum;
}
